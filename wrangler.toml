name = "garmin-proxy"
main = "worker.js"
compatibility_date = "2024-09-23"

# Secrets are NOT defined here — set them via CLI so they are never stored in plaintext:
#   wrangler secret put CONSUMER_KEY
#   wrangler secret put CONSUMER_SECRET
